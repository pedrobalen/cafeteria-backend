spring.application.name=gestao-cafeteria
# ===================================================================
# CONFIGURA��O DO BANCO DE DADOS (DataSource)
# ===================================================================
# A aplica��o vai se conectar em 'localhost' na porta '5432', que � a porta
# que mapeamos no docker-compose.yml. O nome do banco � 'cafeteria_db'.
spring.datasource.url=jdbc:postgresql://localhost:5432/cafeteria_db

# Usu�rio e senha definidos no docker-compose.yml
spring.datasource.username=admin
spring.datasource.password=admin

# ===================================================================
# CONFIGURA��O DO JPA/HIBERNATE
# ===================================================================
# 'update': O Hibernate tentar� atualizar o schema do banco com base
# nas suas entidades Java. �timo para desenvolvimento.
spring.jpa.hibernate.ddl-auto=update

# Mostra no console o SQL que o Hibernate est� executando.
spring.jpa.show-sql=true

# Especifica o dialeto do PostgreSQL para o Hibernate.
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect